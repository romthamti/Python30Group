# เครื่องนับก้าวรายสัปดาห์
total_steps = 0  # ตัวแปรเก็บจำนวนก้าวรวมทั้งสัปดาห์

for day in range(1, 8):  # วนลูป 7 วัน
    try:
        # รับจำนวนก้าวของแต่ละวัน
        steps = int(input(f"วันที่ {day} ใส่จำนวนก้าว: "))

        # ถ้าจำนวนก้าวติดลบ ให้แจ้ง error
        if steps < 0:
            raise ValueError("จำนวนก้าวต้องไม่ติดลบ")

        total_steps += steps  # รวมจำนวนก้าว

    except ValueError as e:
        print("❌ ข้อมูลผิดพลาด:", e)

# แสดงผลรวมจำนวนก้าวทั้งสัปดาห์
print("👟 จำนวนก้าวรวมทั้งสัปดาห์ =", total_steps)